.vercel
node_modules
.env
.env.local
.env*.local

.env*

# dumps do scripts/migrate-kv-to-pg.js — contêm hash de senha de usuários reais,
# nunca commitar (ver ROADMAP.md §6)
backups/

.vercel
.env*

# estado local de sessão do Claude Code (não é do projeto)
.claude/

# .env.example é template sem segredo — precisa ser versionado
!.env.example
